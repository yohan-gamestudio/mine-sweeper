services:
  web:
    build: .
    command: npm run dev -- --host 0.0.0.0 --port 5173
    ports:
      - "5173:5173"
    depends_on:
      - ws
  ws:
    build: .
    environment:
      - HOST=0.0.0.0
    command: npm run server:dev
    ports:
      - "3000:3000"
